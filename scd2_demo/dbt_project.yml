name: scd2_demo
version: 1.0.0
config-version: 2
# require-dbt-version: ["=1.10.15"]

profile: duckdb

model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots

models:
  scd2_demo:
    +materialized: table              